var width = screen.width;
var height = screen.height;

var space = document.getElementById("canvas");
space.setAttribute("width", width);
space.setAttribute("height", height);

var ctx = space.getContext("2d");

//Start to code!
